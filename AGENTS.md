# AGENTS.md

## 最小改动原则

每次对程序的修改、更新或优化，**严格限定在目标问题所涉及的范围之内**。不得以任何方式修改、影响或破坏与当前问题无关的程序功能。

具体要求：
- 只修改与当前问题直接相关的代码路径、组件和模块。
- 不调整与问题无关的样式、布局、配置或行为。
- 不在修复或优化过程中顺手重构无关代码。
- 不改动不相关的依赖版本、构建配置或项目设置。
- 若改动可能产生连锁影响，必须先评估影响范围，确保不会波及无关功能。

唯一可接受的改动是解决当前问题所必需的改动。无法论证必要性的变更，不应出现在变更集中。

## 完成验证原则

每次任务完成后，**必须运行与改动相关的测试或检查命令**，确认两件事：

1. 本次任务的目标已成功达成。
2. 改动未破坏任何已有功能。

具体要求：
- 优先运行项目已有的自动化测试套件。若测试未通过，必须先修复再结束任务。
- 若无现成测试，则运行与改动模块直接相关的检查命令（如语法检查、构建验证、lint 等）。
- 对于 UI 相关改动，应在浏览器中实际验证关键路径的行为是否正常。
- 不可仅凭代码审查或主观判断宣称任务完成——必须有可观测的验证结果作为依据。
